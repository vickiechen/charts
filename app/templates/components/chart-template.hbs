<h2> My Chart </h2>

{{#power-select placeholder="Please, select a chart type" selected=chartType options=chartTypes onchange=(action "changeChartType") as |type|}}
  {{type}} chart
{{/power-select}}

<hr>

{{#if chartData}}
	{{ember-chart type=chartType data=chartData options=chartOptions}}
{{/if}}
